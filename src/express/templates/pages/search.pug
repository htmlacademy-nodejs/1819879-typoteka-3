extends ../layout

block content
  main
    section.search-page
      .search-page__wrapper
        h1 Поиск
        .search.search-page__form
          form(action='/search' autocomplete='/off' method='get')
            label
              input(type='text' name='query' placeholder='Что ищем?' value='Путешествия')
            button.search__button.button.button--transparent(type='submit') Найти
          each article in results
            .search__result
              time.search__date(datetime=article.createdDate)
                +insertTransformDate(article.createdDate)
              a.search__link(href='#') #{article.title}
